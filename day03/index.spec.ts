import {readInput} from "../utils/input";
import {run1,run2} from "./index";

test("day03 - run 1", () => {

    const input = readInput(__dirname);
    run1(input)

});

test("day03 - run 2", () => {

    const input = readInput(__dirname);
    run2(input)

});